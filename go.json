{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Additive policy for Go repositories. Extend both the default and go presets.",
  "postUpdateOptions": [
    "gomodTidy"
  ]
}
